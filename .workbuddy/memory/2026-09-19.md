# 2026-09-19

## 十五轮：Mobile Console 1.1.2 — Concurrency & Schedule Data Boundary（已推送 dev）

按《Potal Mobile Console 1.1.1 GitHub 三次复审暨 1.1.2 修改执行报告》全量执行，基线 `dev@1182982`。11 个功能 commit（`b71281e..9358bb7`，报告 §76 拆分）+ 复审修复 `942ccd3`。

### 功能 commit 概要
- **P0**：新建共享 `useAccessPolicyEditor`（Desktop AccessPage + MobilePermissionEditor）：requestSeq 代际守卫（effect cleanup 失效在途）+ `ready` 的 `policy.application_id === applicationId` 不变量 + save target guard（targetIdRef）+ save generation guard。
- **P1**：`useSchedules` 51 probe / 50 display keyset 分页（`limit=50+1` 探 hasMore，cursor=已展示末行 id）；`q` 服务端搜索（300ms 防抖，筛选/搜索变化回第一页）；`errorPhase: initial|refresh|loadMore` 三相 + 失败保数据。后端：openapi `q` 参数、handler、`ListFilter.Query`、`automation.sql`（`COLLATE utf8mb4_unicode_ci LIKE` + `likePattern` 转义 `%/_/\`，catalog 同款）+ sqlc/oapi 再生成。**limit clamp = 100 ≥ 51，前端探针成立**（复审确认非 P0）。
- **P2**：`useScheduleDetail` schedule/occurrences 两失败域解耦 + 执行记录 51 probe 分页；`useScheduleEditor` apps 错误态 + resolve 404=unavailable（要求重选才能保存）/5xx=transient（可重试）；`useDirectoryUsers` disabled 废弃在途；MobileUserPicker 关闭清 query；Mobile Access/Resource 单一 Retry CTA；Desktop userOptions 合并已授权人员；MobileSync load seq 守卫 + refreshing 禁点。
- **P3**：router lazy 二级管理页（主包 1777→1563 kB，gzip 563→499 kB；WorkspaceHost/主 Chat/Shell 不延迟）；deps 仅 lock 内 semver bump（`npm audit --omit=dev` 零生产漏洞）。

### 双子智能体复审与修复（942ccd3）
- 后端 agent：可推送、无 P0；补集成测试 `TestScheduleListSearchIsServerSideCaseInsensitiveAndLiteral`（大小写不敏感 + `_`/`%` 字面量 + q×before_id 组合），**dev 库（192.168.211.26，MySQL 5.7.32）实跑 PASS**——`config.Load()` 自动读 `backend-go/.env.local`，`STUDIO_TEST_DB=1` 即连 dev 库跑集成测试。
- 前端 agent：2 P1 + 3 P2，全部修复并 resume 复核通过：
  - P1-1 `useApplicationPage` loadMore 楔死**漏修**（#2 同款第四处）→ 无条件复位 + catch 代际守卫。
  - P1-2 关闭→重开首帧闪现旧目标数据 → `useAccessPolicyEditor`/`useScheduleDetail` 关闭时清空（不可见期间执行，在途全被代际守卫拦下）+ occSeq cleanup。
  - P2：AccessPage searchUsers 代际守卫；Drawer 首帧 Skeleton（消灭 Empty 闪现）。
- **新铁律**：① spinner（loadingMore/saving）被 bump 作废后 finally 必须无条件复位——用 seq 守卫复位会永久楔死（loadMore 互斥使无条件复位安全）；② 关闭表面时同步清空上一目标数据，重开不闪现（清空发生在 paint 后 effect，但表面不可见，无窗口）。
- 回归：vitest 62 文件 **538** 用例 + tsc + eslint + build 全绿；go build/vet/test 全绿。

### 遗留（复审标记非阻塞）
schedule detail 配置域重试按钮（§37 未强制）；直切 A→B 一帧窗口（drawer mask 后 UI 不可达，ready 不变量拦写入）；LIKE 无显式 `ESCAPE '\\'`（与 catalog 一致，NO_BACKSLASH_ESCAPES 才失效，待统一）；openapi `q maxLength:200` 服务端未强制（applications 的 q 无此约束，不一致）。

### 本轮踩坑
1. **上轮会话在复审中途耗尽积分**：复审修复只做了一半——AccessPage 解构了 `loading`/`loadError` 却与 `useApplicationPage` 的 `loading` **同作用域重复声明（无法编译）**，且 Drawer 三态 JSX 未写。接手中断任务先 `git status` + `git diff` + tsc 编译验证未提交改动的完整性。
2. PowerShell 无 heredoc：`git commit -m "$(cat <<EOF)"` 语法错误 → 消息写入 `.git/` 下临时文件用 `git commit -F`。
3. 本机 `npx`/`node` 不在 PATH：Node 在 `C:\Users\吴志彬\software\nodejs`，每条命令先 `$env:Path = "C:\Users\吴志彬\software\nodejs;$env:Path"`；Go 在 `C:\Program Files\Go\bin`。
4. 集成测试本机跑法：`backend-go` 目录下 `$env:STUDIO_TEST_DB='1'; go test ./tests/integration/ -run <Test> -v`，config.Load 自动读 `.env.local` 连 dev 库（无本地 MySQL/Docker）。
5. **新增调 `app.Build` 的集成测试必须带 `t.Setenv("APP_ENV","development")` + `t.Setenv("TOKEN_ENCRYPTION_KEY","<dummy>")`**：CI 的 integration job 只有 DB/Redis 变量、无 `.env.local`，`Build` 会在 `crypto.NewAESGCM("")` 处死（"crypto: empty encryption secret"）。上轮 ec7b6bf 新增 `TestAppBuildWiresEnterpriseACLFlagToRepoAndService` 忘了这对 Setenv → backend CI 从 #48 红到 #49（本地绿是因为 `.env.local` 有真密钥，**集成测试本地绿 ≠ CI 绿**）。修法照 `worker_dispatch_test.go:26-34` 惯例（b1d9b12，backend #50 恢复绿）。诊断入口：CI 注解 API `/repos/{o}/{r}/check-runs/{id}/annotations` 带 `::error` 摘要（含失败测试名+行号），无需拉日志。

### CI 终态
frontend #32（911fddb）success；backend #50（b1d9b12）success。二者均为本批提交的最终绿。
