# 2026-09-13

## Git 已提交内容敏感信息审计

**做了什么**：对全量提交历史（3 个提交 / 746 个 blob / HEAD 707 文件）做敏感信息扫描，覆盖私钥、云厂商 AK/SK、数据库连接串、Token、内网地址、PII 等 18 类规则 + 三轮定向复核。

**结论**：无数据库密码 / 私钥 / 云 AK SK 明文；但存在 1 项可用凭据 + 内网基础设施信息 + PII。

**主要发现（按级别）**
- 🔴 `docs/开发进度清单.md` L39 明文列出测试管理员账号口令（`demo`，is_staff）；同一口令还被 3 个测试夹具复用（`backend-go/internal/platform/crypto/crypto_test.go`、归档的 `apps/users/tests/test_display.py`、`e2e_agent_market.py`）
- 🟠 `backend-go/.env.example` 泄露真实内网地址与账号：TiDB `192.168.212.38:6000` / 库 `xiaoan3_go`（旧库 `xiaoan3`）/ 用户 `xiaoanuser`，Redis `192.168.211.239:6380` DB2；另在 `docs/开发进度清单.md`、`docs/archive/Go重构交接提示词.md`、`docs/archive/...Go 重构项目启动提示词.md`、`backend-go/README.md`、`internal/execution/ratelimit_test.go` L27、`internal/platform/config/config.go` L162/176 重复出现。密码字段本身是 `replace-me` 占位，安全
- 🟠 PII：真实姓名「吴志彬」15 个文件、企业工号「19127920」11 个文件（含 `frontend/src/lib/chatIdentity.ts` L49 注释、`chatIdentity.test.ts`、`session_test.go`、`e2e_go_chat.py`）；飞书 open_id `ou_c821bd49...` 在 `docs/开发进度清单.md` L41
- 🟠 39 张已提交截图（`gui-test-screenshots/` 36 + `backend-go/tests/go_e2e_*.png` 3）含真实头像照片、账号标签「吴志彬（19127920）」、真实会话列表与标题
- 🟡 `cmd/testsession/main.go` 可仅凭用户名免密签发真实 studio_session cookie（无口令校验的 E2E 辅助工具），需确保不进生产
- 🟡 飞书租户智能体 ID `agent_4k6wf15ngw7wu`：`backend-go/internal/integrations/aily/adapter.go`

**确认干净**：`.gitignore` 已覆盖 `.env`/`*.pem`/`*.key` 等；真 `.env` 从未提交（历史只有 3 个 `.env.example`，归档那份已 `REDACTED`）；`docker-compose.yml` 全变量插值；归档 Django 里的 `password='testpass'` 等均为占位；`go.sum` 的 `LTAI` 命中是依赖模块路径误报

**关键提醒**：内容已进入 3 个提交，`git rm` 无法抹除历史，需 `git filter-repo` 重写 + 强推，且必须同时轮换凭据（重写历史收不回已外泄口令）

**环境坑**：本机 Git Bash shim 的 PATH 只剩 PortableGit 目录，`head/grep/wc/ls/dirname` 全部 `command not found`（连 `git ls-tree | head` 都会失败）。凡需管道/文本处理，一律走 Python 脚本（`subprocess` 调 git + 自己解析），不要用 shell 管道

**产出**：报告 `C:\Users\吴志彬\Documents\creation_agent_studio-敏感信息审计报告-20260913.md`（**刻意放在仓库之外**，避免报告本身被提交进 git）

## 远程仓库迁移至 shilin414/potal

**做了什么**：`git remote set-url origin https://github.com/shilin414/potal.git`（原为 `https://github.com/soundex-dele/creation_agent_studio.git`），追加提交 `.workbuddy/memory/2026-09-13.md`，推送 `dev` 分支并建立上游追踪。

**结果**：远程 `refs/heads/dev` = `b75b890`，本地与远程一致，工作区干净。新仓库为**空仓库**（克隆后首推）。

**仓库可见性**：**私有**。验证方式 —— `git -c credential.helper= ls-remote` 报 `could not read Username`，且匿名 `GET https://api.github.com/repos/shilin414/potal` 返回 **404**（私有仓库对匿名者返回 404 而非 403）。故已推送的敏感信息未公开暴露，但仍在 GitHub 侧留存。

**⚠️ 与上文审计的关联**：本次推送把这 4 个提交（含审计报告列出的内网地址 `192.168.212.38` / `192.168.211.239`、真实姓名与工号、39 张含真实头像与账号标签的截图、测试口令 `demo`）**一并上传到了远程**。审计建议的 `git filter-repo` 重写历史 + 强推 + 轮换凭据因此变得更紧迫：清理必须同时在本地与远程完成。

**再次命中本机 git 引用写入缺陷**：`git push` 打印 `* [new branch] dev -> dev` 且远程确实收到 `b75b890`，但本地 `refs/remotes/origin/dev` 仍停在旧值 `e8a6991`，`.git/refs/remotes/` 目录为空、`packed-refs` 里是旧 sha，导致 `git status -sb` 误报 `[ahead 3]`。修复：`mkdir -p .git/refs/remotes/origin` + 写 loose ref + `sed` 改写 `packed-refs` 中对应行，之后 `for-each-ref` 与 `status` 均正常。**结论固化：本机任何 `push/fetch/pull` 后都必须复查 `refs/remotes/**`，不能信任 git 自己的输出。**

**环境坑（补充）**：PATH 被 shim 破坏时，临时 `export PATH="/usr/bin:/bin:/c/software/Git/cmd:$PATH"` 即可恢复 `head/grep/sed` 等；curl 在 `/c/software/Git/mingw64/bin`
