# 2026-09-18

## 十三轮：二次复审执行报告落地（P0+P1+P2 全量）

按 `~/Downloads/potal 最新代码二次复审暨剩余问题完整修改执行报告.md` 改，基线 `dev@24fa3a0`，目标 MySQL 5.7。

### 后端
- **P0-1**（前端）：`fetchWorkspaceBootstrap` 路径 `/workspace/bootstrap` → `/v2/workspace/bootstrap`（原来 404；store 单测把 service mock 掉了所以没暴露）。
- **P0-3 / P0-4**：`GetApplication` 加认证 + `VisibleTo`（不可见/不存在统一 404）；avatar 从 `publicRoutes` 移除并加认证 + 可见性，**检查顺序：认证 → 可见性 → ETag/304**。新增指标 `studio_application_visibility_denied_total{surface}` 便于发现「关漏洞把合法调用方打断」。
- **P0-5**：新增正交的 `mode=manage|consume`。`catalog.Usable` = `Enabled && (kind!='chat' || enabled binding)`，与 `AuthorizeExecution` 同一谓词，落在 `ListApplicationPage` 的 SQL 里（不 Go 侧过滤，否则破坏 keyset 确定性）。resolve / mention 恒为 consume；bootstrap 分组 SQL 里恒带。
- **P0-6**：`SetDefaultAgent` 要求 Enabled（错误文案改「已启用且有可用运行时绑定…」）；`Update(enabled=false)` 事务内清 `is_default_agent`；bootstrap default 的 SQL 自带 `enabled=1 AND bound`。
- **P1-1**：Bootstrap 去掉 `bootstrapPoolLimit=5000` 的 Go 内聚合 → 9 条 SQL（default/favorites/frequent/recent/recommended/fixed/两类 categories/按 id 取行）。旧的 `buildBootstrapGroups` 等纯函数删除，测试改为钉新的 SQL→响应胶水（`bootstrapGroupIDs`/`bootstrapUsage`/`bootstrapSummaries`/`bootstrapCategoryRail`）。
- **P1-2**：resolve 改用 `UsageByApplications/FavoritesByApplications([]int64{app.ID})`，不再 `personalUsage` 全量。
- **P2-1**：`applicationListItem` 去掉 `external_resource_id/identity_mode/execution_mode`（provider 资源 id 只留 authoring 的 ApplicationDetail）。
- **P2-3**：新增 `studio_workspace_bootstrap_requests_total` + 上面那个 visibility denied（labelled）。**EXPLAIN 实测未做，需真实 DB。**

### 前端
- **P0-2**：新增 `stores/resetSessionState.ts`（**注册表模式，不 import 任何 store**），`useAuthStore.acceptAuthenticatedUser` 作为唯一装机入口；bootstrap/entity store `clear()` 递增 generation 拦截在途响应。
- **P1-3**：`AppsPage.applyApplicationMutation` 一次改三处（page + entity + bootstrap）。
- **P1-4**：bootstrap 加 `dirty` / `invalidate()`，run 创建成功、智能体增删改、AppsPage 开关都只打标记不重拉。
- **P1-5**：`toggleFavorite` 按 kind 分流（只有 chat 进收藏），并复用后端排序 + `slice(0,8)`。
- **P1-6**：entity store 只吞 404，其余 reject；WorkspaceHost / HomeWorkspace 区分「找不到应用」与「加载失败」并给重试，**不再删用户的 `?conversation=` 深链**。
- **P2-2**：legacy `/agents/` 无服务端分页 → 加渲染预算 24 + 显示更多（真迁移是 Legacy Agent → Application）。

### 踩坑记录
1. **循环依赖（重要）**：`axios → useAuthStore → resetSessionState → useConversationStore → axios`。Vitest 下静默破坏 `useRunChatStore` 的依赖图（mock 的 `createRun` 根本没执行，7 个用例报 "Cannot read properties of undefined (reading '2')"，看起来跟改动毫无关系）。→ reset 文件改为零依赖 + 各 store 自注册。
2. **判基线的正确姿势**：不能用 `git stash`（本机曾删对象库），也不能 `git checkout <sha>`。用 `git worktree add <临时目录> HEAD --detach` + Junction 链 `node_modules`，跑出基线 280/280 才敢确认 21 个失败全是自己引入的。
   - ⚠️ **清理 worktree 时用 `Remove-Item -Recurse` 会顺着 Junction 把真 `node_modules` 一起删**：本次删掉了 `.bin`、`@ant-design/*`、`@asamuzakjp/dom-selector/src/js/evaluator.js` 等，导致 12 个测试套件静默收集到 0 个用例（JSON reporter 里 suite 状态是 failed 但 assertionResults 为空，**总数看着变少、失败数却是 0，极易误判为通过**）。
   - 修复：先 `git worktree remove`，**确保 Junction 已删除**再递归删目录；或删完立刻用 `npm install --no-save` 补齐。npm 对「目录在但内容被删」的包会报 "up to date"，必须**先删目录再装**。`npm ci` 在本机跑了 36 分钟无进展，不如逐个定点补装（每个约 20-30s）。
   - **判环境是否真好的标准：对比基线用例总数（280）而不是只看 FAILED=0。**

---

## 十四轮：三次复审执行文档落地（三批提交，已推送 dev@eb04e0a）

按 `~/Downloads/potal dev 最新代码复审暨剩余问题修改执行文档（c6db4dc）.md` 全量执行，基线 `dev@c6db4dc`，三批分开发布。

### 第一批 dd51bed（后端 Catalog 不变量）
- **P0-R1**：`promoteDefaultIfEligibleTx` 成为事务内唯一晋升入口（FOR UPDATE 读最终态 + enabled/chat/最新 enabled binding/provider active，失败整体回滚）；Create/Update/SetDefaultAgent 全复用，裸 `promoteDefaultTx` 删除。堵死 `PATCH {enabled:false,set_default_agent:true}` 先停用再复晋升的旁路。migration **0026** 清理存量非法 default（按同谓词，dev 库实测 0 条）。
- **P0-R3**：`catalog.Consumable(ConsumptionFacts)` 取代 `Usable`（provider 存在且 active 纳入消费谓词，provider_key join fail closed）；page consume / bootstrap 五组 + categories / rows-by-ids 全部 SQL 化；新增 `GetConsumptionBundle` 单 JOIN（app+当前 binding+provider）供 resolve/mention。
- **P0-R4**：`GET /v2/applications/{id}` 收紧为 `canManageCaller`；`writeApplicationAccessError` 把 ErrNotFound/ErrNotManageable 统一 opaque 404 应用到全部 by-id 管理面（消灭 403/500 存在性探测）；Favorite/Unfavorite 加 `isStaff + VisibleTo`。
- **P1-R1**：resolve/detail/favorite/default-agent/avatar 全部区分 ErrNotFound(404) 与 DB 错误(500)；修掉处理器里 4 处 `x, _ :=` 二次读取（防 nil 解引用）。
- 测试：`tests/integration/catalog_invariants_test.go`（默认智能体 8 项矩阵 + provider kill switch 四消费面一致性 + 顺序 ID opaque），真实 MySQL 5.7.32 全过。

### 第二批 0f16a20（前端会话隔离与竞态）
- **P0-R2**：OrganizationStore 注册 session reset；`resetSessionScopedState` 直接清 `SESSION_SCOPED_STORAGE_KEYS`（organization/workspace-storage；theme 保留）——lazy chunk 未加载时 persist 也不会 hydrate 旧用户数据；axios 拦截器随之读不到旧 X-Organization-ID。resetSessionState.test 升级 jsdom 环境断言真实 localStorage。
- **P1-R2**：bootstrap store 加 `invalidationRevision`（旧响应落在 invalidate 后不清 dirty）；inflight 清理用 request identity（旧会话 finally 不破坏新会话去重）；`load(true)` 先 drain 在途请求再重拉。
- **P1-R3**：收藏 toggle 按 per-app `favoriteVersions` 收敛，失败只恢复 target flag + invalidate，整快照 rollback 删除。四个确定性 Promise 竞态测试。

### 第三批 eb04e0a（SQL 语义 + EXPLAIN 实测 + CI）
- **P1-R4**：page 的 allow_unbound 过滤改为 `(kind<>'chat' OR allow_unbound OR b.id)`，fixed 消费不再依赖 include_unbound（契约测试钉住）；MobileCatalogSheet 删掉 `includeUnbound: type==='app'` 补偿。
- **P2-R2**：applications URL 全部收编 runApi（AgentEditorModal 直连改 `fetchApplicationDetail`，`RuntimeAgentDetail` 类型收编导出）；ESLint no-restricted-syntax 禁止 services 外出现 /v2/applications 字符串（探针验证生效；**dev 上本有 6 个既有 lint error（unused vars/条件 hook），与本次无关**）。
- **P2-R1**：EXPLAIN 矩阵在 dev 库实测完成（MySQL **5.7.32**）：chat 走 `idx_app_catalog_public`、fixed 走 `idx_applications_kind_public`、无补索引证据；runs 聚合 `idx_runs_user_application_created` Using index。工具固化为 `cmd/explain-catalog-matrix`，结论落盘 `docs/potal 三次复审 EXPLAIN 矩阵实测（MySQL 5.7.32）.md`。`user_application_usage` 按报告暂不实施，留触发条件。

### 本轮踩坑
1. **sqlc 反连接 + replace_all 组合坑**：对共享 JOIN 块 replace_all 追加 providers JOIN 时，此前手工改过的 ListApplicationPage 因匹配文本未变被重复追加 → `Error 1066 Not unique table/alias: 'p'`（跑真实 DB 才暴露）。教训：对 SQL 做块级替换后必须数一遍同一 JOIN 出现次数。
2. **TestCreateRunIdempotentResolveIsReadOnly 在共享 dev 库必然失败**（断言全局 `run_requests COUNT(*)=0`，而库上有真实调度在写）——用 `git worktree` 在基线 c6db4dc 复跑确认同为失败（同样 7 条），属既有环境性 flake，与本次改动无关。
3. **本机 PowerShell 写文件默认 CRLF**：补 `refs/remotes/origin/dev` 与 packed-refs 时尾部带 `\r`，git 报 "ignoring ref with broken name …dev?"。必须用 `[System.IO.File]::WriteAllText` 写 LF 结尾。
4. **PowerShell 中文用户名路径**：`go run` 编译的临时 exe 路径含 `吴志彬`，PowerShell 下 fork/exec 报找不到文件 → bash 里 `go build -o xxx.exe && ./xxx.exe` 可靠。
5. vitest 全量 310 项 + tsc --noEmit + go vet/build/test 全绿后才分三批提交；push 后 `ls-remote` 确认远程 sha=eb04e0a，手补 refs/remotes 三件套。
3. **Python 改文件会换换行符**：`io.open(p,encoding='utf-8').read()` + `.write()` 在 Windows 上把 LF 变成 CRLF → 整个文件进 diff（一度显示 7000+ 行改动）。改完必须按 HEAD 的字节形态归一化回去。
3. **sqlc**：`NOT sqlc.arg('x')` 不会生成参数，改 `sqlc.arg('x') = 0`；同一查询内 `runs.user_id`(NullInt64) 与 `application_favorites.user_id`(uint64) 必须用不同 arg 名。
4. 删代码块时切过头（把 mention 排序函数一起删了），用 `git show HEAD:<path>` 取回片段补回。

### 验证
- backend：`gofmt` 干净 + `go build` + `go vet` + `go test ./...` 全绿。
- frontend：`tsc --noEmit` 干净 + `vitest run` **302 passed / 0 failed**（基线 280，新增 22；28 个套件全部收集到用例）。
- 已推送远程 `dev`（`b621c76`），并按本机缺陷手补了 `refs/remotes/origin/dev`（loose ref + packed-refs），`git status -sb` 已显示无 ahead/behind。

### 补充
- P0-4 关掉公开头像后确认过 `SharePage` 用的是 antd 文字 Avatar（'AI'），**没有**引用 `/applications/{id}/avatar`，不会回归。
