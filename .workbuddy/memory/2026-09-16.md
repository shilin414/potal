# 2026-09-16

## 第九轮补丁 3.2 完成（Streaming Range + Provider Identity + Idempotency Error Closure）
按复审报告《potal 第九轮补丁3.1最新代码复审暨3.2修改执行报告》四个子批次执行完毕并推送 dev：
- **3.2-A**：coalescer 加 `totalReceived`，transient delta 带 absolute UTF-8 end offset（与 durable chunk 同一坐标系）；前端 `applyIncrementalRange` 统一 delta/chunk 对账，无 offset legacy delta 保留 append。
- **3.2-B**：client.StartChat 严格校验（malformed/空 chat id → ErrServer APIError）；executor defense-in-depth（SubmitPrepared 空 ExternalRunID → onSubmitFailure park）；`bindThreadAndRun` 拆成 `persistProviderAcceptance`（canonical，失败本地重试 50/100/200ms 后 sentinel `ErrProviderAcceptancePersistence`，classifyError 原样上抛，靠 lease/reaper 收敛）+ `bindProviderSessionBestEffort`。
- **3.2-C**：`serveReplayAfterRefusal` 升级为 `tryServeIdempotentReplay`（found→200 / KeyReused→409 / infra err→500 / not found→原错误），挂到 authorize/admitUserRun/conversation/attachment 四个 post-miss 拒绝出口。
- **3.2-D**：openapi.yaml 顶部 SSE 说明改 durable cursor 语义；RunEvent enum 补 `run.cancelled`。

## 新增测试与反证
- 后端：`tests/integration/review9_patch32_test.go`（3.2-B 三个集成测试 + 12.3/12.4 两个真实事务 replay 测试）；单测 `TestDeltaCoalescerSharesOneByteCoordinateSystem`；单测 12.1/12.2。
- 前端：reverse-overlap describe（Test1–4 + legacy）。
- 注入手法：MarkSubmissionAcceptedOwned 失败用「holder 事务锁 runs 行 FOR UPDATE + tuned session `innodb_lock_wait_timeout=1`」注入真实 1205；runs.id 是 binary(16)，SELECT 别 scan 进 int。
- 新反证脚本 `backend-go/scripts/falsify_review9_patch32.sh`（4/4）+ `frontend/scripts/falsify_review9_patch32.sh`（3/3）。旧脚本回归：backend 15/15+16/16、frontend 14/14 全绿。
- **锚点同步**：3.2 改了 delta case / applyIncrementalRange 签名，旧脚本 mutation 7 的锚点已同步更新（falsify_review9.sh 两处、frontend falsify_review9.sh 一处）。

## 踩坑
- **前端反证脚本第一次跑时把 `cp` 写成了不存在的 `snapshot` 函数 → 无 .orig → mutated 文件留在工作区，修复时用 `git checkout --` 还原把未提交的 3.2-A 前端修改一起冲掉了**，重新 Edit 应用。教训：反证脚本先 `bash -n` 检查、helper 名要与模板一致；mutated 后立即验证还原。
- verdict 对 vitest 用 `-t "title"` 过滤时，grep "failed" 判 FAIL（"Tests 1 failed"）。

## CI
- 3.2 提交 `e43cc0e` 推送 dev；backend run **35005799280**、frontend run **35005799297** 全绿。本地 refs/remotes/origin/dev 已按惯例手工修复并 ls-remote 三方核对。

## 本地开发环境启动（可复现配方，2026-09-16 实测）
四进程拓扑（端口已确认无冲突，前端用 3030 与 `.env.local` 的 `FEISHU_REDIRECT_URI` 对齐）：
```bash
export PATH="/usr/bin:/bin:/c/software/Git/cmd:/c/Program Files/Go/bin:$PATH"
cd backend-go && go build -o ./bin/{api,stream,worker,migrate}.exe ./cmd/<name>
./bin/migrate.exe                       # 幂等，已在 23 基线时只打印 "migrations applied"
./bin/api.exe      # :8080    studio-api listening
./bin/stream.exe   # :8081    studio-stream listening
./bin/worker.exe   # 无监听端口，日志 "worker consuming" -> feishu_aily
cd frontend && ./node_modules/.bin/vite --port 3030 --strictPort   # 全部 run_in_background
```
- **必须 build 出 exe 再跑**（`go run` 在本机易被杀软拦）；`bin/` 目录不存在要先 `mkdir -p`。
- **:9090 METRICS_ADDR 不监听是正常的**：`config.MetricsAddr` 只存在于 config，api/stream/cmd 都没起 metrics server，别当故障排查。
- 健康检查：`/:8080/healthz` 与 `:8081/healthz` 都返回 **401**（不是 200），用 401 当作"活着"的判据；真正的活体验证走业务接口。
- **前端登录页已改为飞书唯一入口**，本地账号要点「管理员登录」展开表单，选择器是 `input#admin-login_username` / `input#admin-login_password`（**旧的 `input#login_username` 已失效**，skill `cas-dev-verify` 里那套写法要更新）。账号 `demo / Creator@2026`，提交后 URL 变 `http://localhost:3030/`。
- 无头验证：`/c/software/miniconda3/envs/py311/python.exe` + `p.chromium.launch(channel="chrome")`，`goto` 用 `wait_until="networkidle"` 才能等到 antd 表单挂载。
- 端到端口径证据：Vite 3030 → `/api/auth/login/` 200 → `-b cookie` 取 `/api/auth/me/` 200 → `/api/v2/runtimes` 200 / `/api/v2/applications` 200；`/api/conversations/` 返回 `[]`（demo 名下确实无会话，不是故障）。

### 遗留问题：dev 库有 925 条 itest 应用残留（**未清理，待用户确认**）
`GET /api/v2/applications` 共 927 条，其中 `itest-` 前缀 **925** 条（`itest-authz-<nano-ts>-pub|-priv|-dis`，来自历次 authz 集成测试），首页「常用应用/推荐」被这些卡片刷屏。
真实应用只有 2 条：`creative-chat`(创作助手)、`wenshuxiaoan`(问数小安)。
`backend-go/scripts/` 下**没有**现成清理脚本，需要新写（按 slug `LIKE 'itest%'` 圈定，注意 applications 被 conversations/runtime_bindings 引用，要按 FK 顺序或先查依赖）。清理前必须确认，且本机无 `mysql` 客户端，要写临时 Go 程序或 pymysql。
同理 worker 启动时会 drain 上一会话遗留的 outbox，日志里成片的 `run_config_invalid: runtime snapshot: external_resource_id missing` 与 `run gate killed run (application/binding disabled)` 都是历史脏 run 收敛，不是新故障。

## 第九轮补丁 3.3 执行完成（Provider Capacity + Streaming Deployment Closure）
基线 `e43cc0e`（3.2）。按执行报告 §四十二 顺序做完 3.3-1…3.3-9，报告见
`docs/potal 第九轮补丁3.3整改变更报告（Provider Capacity + Streaming Deployment Closure）.md`。

- **3.3-A 有效容量**：`provider_submissions`（durable 副作用账本）进入准入事实，不新增第二张
  uncertain-slot 表。`CountProviderEffectiveInflight{,ExcludingRun}` = **UNION**（不是 UNION ALL，
  正常运行 slot+submission 是同一执行）of live slots ∪ non-settled `sending/unknown/accepted`；
  排除 `rejected`（4xx 容量泄漏）与已 settled run（账本 accepted 是历史）。admission 必须
  **exclude self**，否则 unresolved run 自拒 → self-deadlock。
- migration 0024 只加索引 `idx_provider_submissions_capacity(provider,state,run_id)`；实测
  up → 二次 up no-op(version=24 dirty=false) → down(-1) 索引消失 → 再 up 恢复。
- `Depth()` 改指 effective；新增 `ControlledDepth()`/`UncontrolledDepth()`；旧注释
  "active slots 是 STRICT 上界" 已按报告 §十九 改写为"unresolved grace 内保守上界"。
- **3.3-B 协议协商**：`stream_protocol=2` 才发 transient `content.delta`（只抑制这一种帧，
  不一刀切 sequence=0）；响应头 `X-Studio-Stream-Protocol` 仅调试用；前端每条连接都发该参数
  （与 `after` 正交，不折进 Last-Event-ID）。发布合同 **Backend first / Frontend second**，
  已写进 `backend-go/README.md`。
- 测试：新增 17 个 Go 测试函数（容量 Case 1-8、SSE legacy/v2/cursor/terminal、协商单测、
  telemetry 指标、slots 透明度）；前端 runStream +2 用例。
  **适配**：`TestSSETransientFrameCarriesNoResumeId` 改为带 `stream_protocol=2`（它断言 transient
  帧的 `id:` 行，legacy 客户端按新契约不再收 transient 帧）。
- 反证 `scripts/falsify_review9_patch33.sh` 4 mutation → **8/8 ok**，无残留。
- CI：backend gofmt/vet/build/`go test ./...` 绿 + 集成 `ok 107s`；frontend tsc/vitest(17 文件
  156 用例)/vite build 绿。`-race` 本机仍不可用（无 gcc），交 CI。

### 踩坑（本次新增，复用价值高）
- **Redis pub/sub 不为未来订阅者缓冲**：SSE 集成测试里"发一个 marker 帧证明网关已订阅"如果只发一次，
  marker 会落在「响应头 flush 之后、网关 SUBSCRIBE 之前」的窗口里永久丢失。必须**循环重发直到收到**。
- 既有 SSE 测试要区分"测 transient 帧本身"（需声明 protocol 2）与"测 legacy 兼容"（不声明）。
- **提交与 CI**：`c1411ee` 推送 `origin/dev`（`git ls-remote` 核对一致）。backend run **35043737965**、frontend run **35043737889** 均 success。
  推送后本机 `status -sb` 又出现 `[ahead 1]`（已知缺陷）→ `mkdir -p .git/refs/remotes/origin` + 写 loose ref + 双写 packed-refs 后恢复正常。
- ⚠️ 工作区里还有**另一个并行会话**留下的未提交前端调试产物：`frontend/src/components/Schedules/ScheduleEditorModal.tsx`（含
  `console.log('DEBUG_VALUES'/'DEBUG_CHANGE'/'DEBUG_ONSELET')`）、`frontend/src/components/Schedules/__tests__/deliveryPayload.repro.test.tsx`、
  `frontend/vitest.repro.config.ts`（文件自注「仅用于一次性复现脚本，不入库」）。**本次提交刻意未包含它们**，别误当作 3.3 的遗留。

### 前端 P0 修复：定时任务飞书投递 `target_type` 丢失（另一并行会话遗留已收口）
- **症状**：定时任务弹窗里选了「投递目标」，创建后 `POST /api/v2/schedules` 返回
  `delivery target_type must be user or chat`（编辑保存 PATCH 同样中招）。
- **根因**：`ScheduleEditorModal.handleOk` 用 `await form.validateFields()` 的返回值拼 payload。
  rc-field-form 的 `validateFields()` / `getFieldsValue()`（**不带 `true`**）只返回**已注册 Form.Item
  的路径**（内部按注册路径 `cloneByNamePathList` 重建对象）；投递只注册了 `deliveries[0].target_id`，
  于是 `target_type` / `target_name` 被静默丢弃；`misfire_policy` / `deadline_policy` /
  `execution_window_seconds` 没有 Form.Item → 同样丢失（此前只是靠后端默认值蒙对）。
- **诊断要点（别被误导）**：浏览器/测试里看到的"值被回滚"是假象——store 本身一直是对的
  （`getFieldsValue(true)` 可见三元组齐全），丢失发生在**读值时**。把 store 写入序列打出来即可区分
  「写入被覆盖」与「读值丢字段」两类问题。
- **修复**：弹窗新增 `readValues()` = 先 `validateFields()` 只做校验，再 `getFieldsValue(true)` 取全量；
  `handleOk` 与 `refreshPreview` 统一走它（创建/编辑两条路径一起修好）。
- **验证（自证 + 反证）**：无头 jsdom 渲染弹窗、真实点击 antd Select 选项、抓
  `createSchedule`/`updateSchedule` 的真实 payload → 报文含
  `deliveries:[{target_type:"chat",target_id,target_name,content_mode}]`；
  反证（把 `readValues` 还原成旧写法）→ **两条用例同时 FAIL**，报文正好退回 `[{target_id,content_mode}]`。
- **新增产物**：`frontend/src/components/Schedules/__tests__/scheduleEditorPayload.test.tsx`（2 用例）；
  `frontend/vitest.config.ts` include 放开 `.test.tsx`；`jsdom` 加入 devDependencies
  （组件测试专用，文件头 `// @vitest-environment jsdom`，默认环境仍是 node）。
- **清理**：上一节记的「并行会话遗留」——`ScheduleEditorModal.tsx` 里的 `DEBUG_*` console、
  `__tests__/deliveryPayload.repro.test.tsx`、`vitest.repro.config.ts` ——**已在本轮回滚删除**；
  诊断期间临时改过的 `node_modules/rc-field-form/lib/useForm.js` 已按 md5 还原
  （`c2d3cb938fa8e50dc190dd764be8e3c6`）。工作区前端只剩上面 4 处有意改动。
- **CI（本机复跑）**:`npx tsc --noEmit` 绿；`npx vitest run` **18 文件 / 158 用例**绿；`npx vite build` 绿。

### 推送与 CI（f058743 + 1043bec → origin/dev）
- 两个提交一并推送：`c1411ee..1043bec`。**本机 ref 缺陷第八次命中**：push 打印成功、远程已收到，
  但本地 `refs/remotes/origin/dev` 仍停在 `c1411ee`（`status -sb` 会误报 `[ahead 2]`）；
  按既定手法（`mkdir -p .git/refs/remotes/origin` + 写 loose ref + 双写 packed-refs）修复后
  remote = HEAD = local ref = `1043bec`，`for-each-ref` 现场打印留证。
- **workflow 是路径过滤的**（记牢，省得找"丢失的"run）：`backend.yml` 只吃
  `backend-go/**`，`frontend.yml` 只吃 `frontend/**` → 本次只触发 frontend run
  **35044964086**（head `1043bec`）**success**，job `check` 每步全绿（npm ci / typecheck /
  unit tests / production build）。`.workbuddy/**` 不触发任何 CI，所以 memory 提交可以放心单独推。
- 工作区仍有一个**非本会话**产物：`gui-test-screenshots/s0*.png`（3 张启动截图，未跟踪、未提交）。

### 用户报告：定时任务跑完、飞书群收不到消息（两层根因）
- **现象**：新建定时任务（target=群 `oc_c9adcca…`/巡检群）→ 对话成功、有回复，但群里没消息。
- **根因 1（用户可见的那一层）**：**没有 `--provider=feishu_delivery` 进程**。投递是独立队列：
  Run 成功 → `Dispatcher.OnRunSucceeded` 建 `delivery_executions`(pending) + outbox → Relay 投到
  `queue:feishu_delivery` → 只有 delivery 消费者会消费。实测 DB：occurrence 33471 `succeeded`、
  投递行 `status=pending / attempt=0 / 无 error`。README「三进程」与 `cas-dev-verify` 技能「四进程」
  都漏了这个进程，已修正为**五进程**（api/stream/worker(aily)/worker(delivery)/vite）。
  - 补：消费者一起，`dueScanLoop`（每秒扫 pending due）**自动补发**，无需手工重放 stream；
    启动前先确认队列里没有指向真实群的陈旧任务（这次全是 `sender_user_id=42` 的假目标 `oc_test`/`ou_test`，
    指向真实群未终态投递 = 0，安全）。
  - **真结果**：09:50:15 该投递被消费 → `status=succeeded, attempt=1`（`SendIMMessage` 只在飞书
    `code=0` 时返回 nil ⇒ 消息确实已发出，只是用户查得比消费早）。
- **根因 2（排查中发现的真缺陷）**：`CASFinishDelivery` 里 `sent_at = IF(? = 'succeeded', ...)` 的第二个
  占位符被 sqlc 生成为 `Column5`，而 worker 成功路径只传了 `Status`/`ID` → `IF(NULL='succeeded')` 为假
  → **成功投递的 `sent_at` 永远是 NULL**（UI 上看就是"已投递但没有发送时间"）。
  修复：两处调用点补 `Column5: <status>`（`internal/delivery/worker.go`）。
  - 验证用**真实 MySQL + 可回滚事务** A/B：旧写法 `status=succeeded / sent_at=None`（复现），
    新写法 `sent_at=2026-09-16 09:53:24.902`，ROLLBACK 后无残留。
  - 该 SQL 的"同名占位符必须传两次"是坑，已在调用点写注释说明。
- **本次改了哪些文件**：`internal/delivery/worker.go`（sent_at 修复）、`backend-go/README.md`
  与 `ENTERPRISE.md`（补投递面进程与 4→5 进程启动命令）。`go build ./...` / `gofmt` / delivery 单测全绿。
- **另一个实测坑**：`(go run ./cmd/worker … &)` 这种自 detach 在普通 Bash 调用里**活不下来**（命令返回即被
  回收，日志停在那秒）；常驻必须用工具的 `run_in_background: true`（本次已用它把 delivery 消费者挂住）。

### 第九轮补丁 3.3.1 执行（Provider Capacity Hot-Path Scale + SSE Protocol Bound）
- **输入**：`~/Downloads/potal 第九轮补丁3.3最新代码复审暨3.3.1修改执行报告.md`。3.3 语义 PASS，
  剩两个 P1：①容量 remote leg 仍 `provider_submissions` 历史驱动（准入在每次 Acquire 上跑）；
  ②`stream_protocol` 回显任意正整数 → 无界 Prometheus label。报告要求**不新增 migration**。
- **3.3.1-A**：`db/queries/execution.sql` 三条容量查询改成
  `runs(active) STRAIGHT_JOIN provider_submissions`，状态过滤从 `NOT IN` 改为显式
  `IN ('queued','running','waiting_input','waiting_external','cancelling')`（= `IsSettled` 补集；
  `status` 是 `idx_runs_claim` 前导列，`NOT IN` 用不上索引区间）。exclude-self 两条腿都保留。
  **不要 FORCE INDEX**：实测已稳定选中 `idx_runs_claim` → migration 保持 24。
  - **sqlc 坑**：`r.id <> ?` 会让生成字段从 `RunID_2` 改名成 `ID`，连带破 `slots.go` 与
    3.3 反证脚本锚点；改写成 `ps.run_id <> ?`（join 是 `ps.run_id = r.id`，等值传播下同一谓词）
    即可保住参数名。
- **3.3.1-B**：`StreamProtocol` 改成协商 —— `requested >= 2 → 2`，缺失/乱码/非正/Atoi 溢出 → 1。
  三处只剩 1/2：返回値、`X-Studio-Stream-Protocol`、`studio_sse_stream_protocol_total{protocol}`。
- **EXPLAIN 必须带真实数据形状**（本机踩到）：空 provider 上所有候选索引都估 1 行，MySQL
  按 tie-break 选 `idx_runs_external` / `uniq_provider_submit_key`（前导列只有 provider），
  EXPLAIN 说明不了生产。先造 5k settled + 2 活跃再 EXPLAIN：
  - 改前 `ps type=ref key=uniq_provider_submit_key key_len=258 rows=2251` → `r type=eq_ref key=PRIMARY`
  - 改后 `r type=range key=idx_runs_claim rows=7 (Using index)` → `ps type=ref key=PRIMARY rows=1`
  - 结论：候选范围来源换了，与历史量无关。**MySQL 的 EXPLAIN `table` 列在有别名时返回别名**
    （`r`/`s`/`ps`），所以断言要先把别名映射回表名（从查询文本解析 FROM/JOIN），
    前导列则查 `information_schema.STATISTICS`（按 shape 断言，不按 key 名）。
- **测试**：新增 `tests/integration/review9_patch331_test.go`（3 个：状态清单互补 / 10k 历史下
  计数 2-1-1 且 max=2 拒、max=3 准入同一个 run / EXPLAIN 计划）；`sse_protocol_test.go` 新增
  协商下界与**基数测试**（16 个恶意输入 → registry 该 family ≤2 series；放 sse 包因为不变量
  的两半只有这里同时可见）；`metrics_test.go` 补 label 形状。
- **反证**：新增 `scripts/falsify_review9_patch331.sh`，3 mutation / 8 判定全绿；3.3 原脚本
  4 mutation 仍全绿（不替换）。
- **结果**：gofmt/vet/build/单测绿；集成全套 111s **PASS**；前端 tsc + vitest 18 文件 158 用例 +
  vite build 绿（前端零改动）。TiDB EXPLAIN **未执行**（本机只有 MySQL 5.7 可授权；20337 有服务
  但凭据未知）→ 列部署验收项。
- **并发会话警告**：本轮期间有**另一个会话在同一工作区**同时工作（09:52–09:56 改
  `internal/delivery/worker.go`(sent_at 修复)/`ENTERPRISE.md`/`backend-go/README.md`，并推了
  c55f24c）。`backend-go/README.md` 是 3.3.1 要求改的文件 → 被两边同时改，**同一文件无法拆提交**；
  已问用户，选择「全部一起提交」。以后在此仓库开工前先 `git status` + 看文件 mtime，避免踩到别人。
- **交付**：commit `adc5621` `fix(review9.3.1): drive capacity admission from active runs, bound the
  stream protocol`；`c55f24c..adc5621` 已推 origin/dev。**本机 ref 缺陷第九次命中**
  （push 打印成功、远程已收到，本地 `refs/remotes/origin/dev` 仍停在 c55f24c →
  `status -sb` 误报 `[ahead 1]`），按 `mkdir -p .git/refs/remotes/origin` + 写 loose ref +
  双写 packed-refs 修复后 remote = local ref = HEAD = adc5621。
- **CI**：backend run **35047359734 success** —— job `check`（actionlint/gofmt/vet/build/unit
  + `go test -race (execution plane)`）与 job `integration`（mysql 5.7 + redis 7、迁移跑两次
  第二次必须 no-op、database-backed 包测试、integration tests）全绿。frontend workflow 路径过滤
  只吃 `frontend/**`，本补丁前端零改动 → **不会触发** frontend run（别去找"丢失的"run）。
- **注**：`docs/**` 与 `.workbuddy/**` 不触发任何 CI，所以报告/记忆的收尾提交可以放心单独推。
- **本轮还踩到**：`sleep`/`head` 在 PATH 被收窄时报 `command not found`（老问题，命令前
  `export PATH="/usr/bin:/bin:/c/software/Git/cmd:$PATH"` 必加）；`go run /tmp/xxx.go` 在 Windows
  下会 `GetFileAttributesEx /tmp/...` 失败，用仓库内临时目录或 `run_in_background`。

## 第十轮 Batch 4 — SSE Hub（执行报告 `potal Batch 4 — SSE Hub`）

- **交付**：`internal/transport/sse/{hub,hub_cache,hub_subscription}.go` + `sse.go` 改为 Hub
  subscriber；`Gateway.Redis` **删除**；`App.SSEHub` + `Close()` 顺序 Hub→Redis→DB；
  `config.SSEConfig` + 5 个 `SSE_*` 环境变量；telemetry 新增 10 个 Hub 指标 +
  `SSEReplayEventsTotal` 语义收窄；CI race 门加入 `transport/sse`；无 migration、前端零改动。
- **验证**：sse 包 **46/46** 单元测试绿；`tests/integration` 真实 MySQL5.7+Redis7 **202 顶用例
  连续 3 次全绿**（新增 Case A 两流共享 1 upstream / Case B v1+v2 混合协议 / Case C 断线重连
  after=cursor 无重复无丢失）；反证 `scripts/falsify_review10.sh` **14/14 ok**（7 mutation）。
  本机无 cgo → `-race` 只能由 CI 兜（已进门）。
- **自证抓到的 3 个真问题**：①初版在 live 循环里推进 `lastDelivered`，把既有 3.3 用例
  `TestSSECursorSemanticsIgnoreStreamProtocol` 打挂（推进会让"低于已投递值的 live durable 帧"
  被丢）→ 回到改造前语义并在代码写明原因；②SUBSCRIBE 失败路径必须 `failUpstream()` 先于
  `closeReady()`，否则 ready 唤醒的请求会给正在拆除的 Hub 注册 subscriber；③`WaitReady`
  语义是"注册已尘埃落定"而非"成功"（成功与否问 `Serving()`），测试初版按后者写会误判。
- **⚠️ 新踩坑（工具级，已写进 MEMORY.md）**：**同一文件并行发两个 Edit 会丢更新**。工具各自
  基于同一旧快照写回、后写覆盖前写，且**代码仍然编译通过**——本轮静默吃掉
  `app.go` 的 `SSEHub: sse.NewHubManager(...)`（Hub=nil 时只是"没有 live 事件"，日志/指标都
  不报错）、`sse.go` 的 `RunReader` 接口、`config.go` 的 `SSE: SSEConfig{...}`。改同一文件
  必须串行 + 改完 `grep` 复核。
- **⚠️ 反证脚本会改真实源码，绝不可与其它 `go test` 并发**：本轮一次全量集成与
  `falsify_review10.sh` 重叠运行，报出一个与 SSE 无关的 schedule/reaper 失败；顺序重跑 3 次
  全绿。已在该脚本头部写明警告。
- **探针帧不是游标**：`syncStream` 用 seq 9000 的 marker 证明网关已订阅，它占用一个真实
  durable 序列；Case C 的游标必须只取 `content.chunk` 的序列（`contentCursor()`），否则重连会
  从 9000 恢复、后续 4/5 被去重丢掉。
- **同包重复 helper 清理**：`review9_patch33` 的 SSE 读取器抽成 `openStreamAt`（放在
  `review10`），让"一个 server 多条流共享 Hub"的用例不需要第二份帧解析器；集成包已有
  `itoa(int64)`，别再定义。



---

## 第十轮 Batch 4.1（SSE Hub Correctness Closure）— 执行完毕并冻结

按 `~/Downloads/potal 第十轮 Batch 4 最新代码复审暨 Batch 4.1 修改执行报告.md` 执行，基线 `f5fc151`。
提交：`c5ac5f2` P1-1 gap repair / `970041e` P1-2 cache 字节硬上界 / `f3cd4bf` P2 代际围栏 + 锁序 /
`521fc4b` 反证驱动。报告 `docs/potal 第十轮 Batch 4.1 整改变更报告（SSE Hub Correctness Closure）.md`。

- **P1-1 durable live 顺序**：`lastDelivered` 收紧为"已连续写出的最高 durable sequence"。live 帧
  `> last+1` 一律拒绝，先 `repairDurableGap` 从日志补 `[last+1..seq]`；补齐失败 fail closed
  （一帧不写、结束连接）。新增 `studio_sse_live_gap_repair_total{result=repaired|failed}`。
- **P1-2 cache 硬上界**：`ApproxBytes > CacheMaxBytes` 的帧不缓存（不截断不豁免），`evictLocked`
  允许清空 ring；拆出 `lastObservedSeq` 与 `lastSeq`。删掉旧契约测试 `newest event always survives`。
- **P2**：`reportCache` 加 hub identity 校验；`GetOrCreate` 改 loop 让 `manager.mu` 与 `hub.mu`
  不再嵌套（`newRunHub` arm idle timer 也去掉了 hub.mu）。
- **本批真正的发现**：手造 durable SSE 帧是**契约负债**，收紧顺序语义会同时打断一组集成 fixture
  （`syncStream` 的 seq 9000 探针、9100/9200/9001/100/101 等伪造序列）。修法：探针改 transient 帧；
  durable 一律 `appendLiveEvent` 落库；乱序用例用 `seedDurableEvent` 显式插行 + 手工 `publishDurableFrame`。
- **证据**：单测 57 tests、集成 SSE 20 tests、反证 14/14 + 8/8（`falsify_review10.sh` 的 A/F 锚点已随
  代码位移同步更新）、gofmt/vet/build/tsc 全绿。`-race` 仍只能由 CI 兜（本机无 gcc）。
- **⚠️ 新踩坑**：①本机 `core.autocrlf=true` 撞本仓库 LF 索引 —— git 一重写文件工作区就变 CRLF，
  `gofmt -l` 全文件报警（索引仍 LF，CI 不受影响）；已把该仓库局部设 `core.autocrlf=false`。
  ②**不要为对照基线 `git checkout <sha>`** —— 实测被 SIGTERM 打断留下半成品工作区（文件被删 +
  `.git/index.lock`）；恢复靠 `rm .git/index.lock` + `git reset --hard HEAD` + 从仓库外备份还原。
  对照基线用 `git checkout <sha> -- <路径>`。③本机两个集成用例对负载敏感（`available_at` 需 ≤50ms，
  实测 60–110ms），A/B 交替已证**基线同样失败**，与本批无关。

---

## 第十轮 Batch 4.1.1 — Hub Initial Timer Race Closure（执行完毕，已推 dev）

按 `potal 第十轮 Batch 4.1 最新代码复审暨 Batch 4.1.1 修改执行报告.md` 执行。基线 `05e74c4`
（生产代码基线 `f360fac`）。提交 **065f204 / 26fae5d / edf496b / 9f45863 / 95e0a60 / 721a913**，
dev 已到 `721a913`（CI backend run 35074005255 对 `95e0a60` 的 check（含 `-race`）+ integration
全 success）。

### 做了什么
- **NEW P1（初始 Idle Timer 竞态）**：Batch 4.1 为拆 `manager.mu → hub.mu` 嵌套，把 idle timer 留在
  `newRunHub` 里且不取 `hub.mu`，但 `armIdleTimerLocked` 会 `time.AfterFunc`，其 callback
  `evictIfIdle` 在别的 goroutine 上清 `idleTimer`/置 `closed`；`SSE_HUB_IDLE_TTL` 只校验 `> 0`，
  `1ns` 合法 ⇒ callback 能赢过赋值，GetOrCreate 会发布已取消的 Hub。新不变量
  **「Unpublished RunHub is inert」**：constructor 只分配结构 + 建 context；publish 之后
  `armInitialIdleTimer()`（内部取 `hub.mu`，`closed` 或 `subscribers != 0` 就 return）。`GetOrCreate`
  创建分支改成 `m.hubs[runID]=hub` → unlock → `go runUpstream()` → `armInitialIdleTimer()`。
- **NEW P2（canonical 日志连续性）**：`repairDurableGap` 原来只判 `<= last` / `> through`，
  会跨过日志自身的洞。加 `ev.Sequence != last+1 → LiveGapFailed` 并停止；**已发出的连续前缀不回滚**
  （SSE frame 撤不回），客户端从实际收到的最高连续 cursor 重连。同步修正 Batch 4.1 报告里
  AC-4.1-5 的「什么都不写」措辞（那只对第一笔 read 失败成立）。
- 测试 +6：`hub_test.go` 4 个（inert / arms / concurrent-cancel / tiny-1ns-race）、
  `sse_hub_test.go` 2 个（canonical hole fail-closed + **gap-free 反向对照**——没有对照，
  「repair 拒绝一切」也能满足洞测试）。sse 包 57 → 63 tests。
- 新反证驱动 `backend-go/scripts/falsify_review10_patch411.sh`：Mutation L（把 timer 放回
  constructor）、Mutation M（删连续性检查）。**5/5 通过**；旧驱动回归 `falsify_review10.sh` 14/14、
  `falsify_review10_patch41.sh` 8/8，锚点未失配。

### 本机验证
`gofmt` empty / `go vet ./...` / `go build ./...` / `go test ./... -count=1` 全 PASS；
`go test ./tests/integration/... -count=1`（`STUDIO_TEST_DB/REDIS=1`）PASS 127s；
前端 `tsc --noEmit` + `vitest run`（158 tests / 18 files）+ `vite build` 全绿。
`-race` 本机仍跑不了（`CGO_ENABLED=1` 实测 `gcc not found`），靠 CI。

### 复用到的坑（本次都踩到/用上了）
①**本机 git 远程引用写入缺陷再次复现**：`git push` 打印 `05e74c4..95e0a60 dev -> dev`、
`git ls-remote` 也确认远程收到，但本地 `refs/remotes/origin/dev` 仍停在 `05e74c4`，
`git status -sb` 误报 `[ahead 5]`。修复照旧：`mkdir -p .git/refs/remotes/origin` → 写 loose ref →
双写 packed-refs。**push 后必须 `git ls-remote` + `for-each-ref` 双查**，别信 git 自己打印的成功。
②**反证脚本改代码后要复核旧脚本锚点**：本次先 `grep` 确认旧驱动没有引用被改动的
`newRunHub` / `idleTimer` 区域，再实跑两个旧驱动确认锚点仍匹配。
③**集成测试不要和前端 build 并发跑**（本机 `available_at` ≤50ms 两个用例对负载敏感）；
本次刻意串行，集成一次通过。
④`MEMORY.md` 已经涨到 ~13KB，注入时尾部被截断。本轮重写压缩（当前 ~13.3KB，删了过时表述、
合并重复条目），并把「`newRunHub` arm idle timer 不取 `hub.mu`」这条**已失效**的规则换成新不变量。
后续仍需继续瘦身。

---

## 第十轮 Batch 4.1.2 — Initial Arm Interleaving Test Closure（test-only，同日晚）

复审报告（`Downloads/potal 第十轮 Batch 4.1.1 最新代码复审暨 Batch 4.1.2 测试加固执行报告.md`）
判定 P0 0 / P1 0 / P2 1：生产实现正确，缺的是 **AC-4.1.1-5 另一半的确定性测试证据**。
本轮**未改任何生产文件**。

### NEW P2 的实质
`TestConcurrentSubscriberCancelsInitialIdleTimer` 的注释声称"subscriber 严格早于 arm"，
但它的 `<-created` 只有在 `GetOrCreate` **完整返回后**才收到值，而返回顺序是
`registry insert → unlock → go runUpstream → armInitialIdleTimer() → return hub`。
所以它真实覆盖的是「arm 已发生 → Subscribe 停表」（交错 A），**建立不了交错 B**
（subscriber 先到 → arm 必须 return 不 arm）。`if len(h.subscribers) != 0 { return }`
这个分支此前没有任何确定性测试钉住；即使被删，旧测试与会话级 `-race` 都会继续绿
（删 guard 不产生 data race，只是把 reuse window 钉在一个"早于 subscriber 存在"的 deadline 上）。

### 改动（3 个文件，均为测试/文档）
- `backend-go/internal/transport/sse/hub_test.go`
  - 新增 `TestArmInitialIdleTimerSkipsAlreadySubscribedHub`：同 package 直接构造状态 ——
    `newRunHub` → 在 `mgr.mu` 下写入 `mgr.hubs[runID]`（= registry insert，**跳过 arm**）→
    `Subscribe` → `armInitialIdleTimer()` → 断言 **`hub.idleTimer == nil`**（根因断言，与调度和
    IdleTTL 无关）→ sleep 2×IdleTTL 断言 Hub 仍在且 `DropReason() == ""` →
    `sub.Close()` 后必须重新 arm 并回收（证明跳过 arm 没把 lifecycle 绕掉）。
    `IdleTTL = 250ms`：足够长，使"被 arm 了"表现为非 nil 字段而不是一次与断言竞争的回收；
    又足够短，使 detach 后的回收仍在 `waitFor` 的 3s 窗口内。
  - `TestConcurrentSubscriberCancelsInitialIdleTimer`
    → **改名 `TestSubscriberStopsArmedInitialIdleTimer`** 并改写注释，不再声称覆盖交错 B。
    测试保留（它仍有价值）、**未删除、未放宽**。
- `backend-go/scripts/falsify_review10_patch411.sh`
  - 新增 **Mutation N**：删掉 `armInitialIdleTimer` 的 subscriber guard。
    实测 **mutated FAIL / restored PASS**；`INERT_TESTS` 里同步改名。
- `docs/potal 第十轮 Batch 4.1.1 整改变更报告（Hub Initial Timer Race Closure）.md`
  - §4.1 表格纠名 + 加纠偏说明；新增 **§10 Batch 4.1.2**（判定、改动范围、测试设计理由、
    Mutation N、本地证据、AC-4.1.2-1..9 对照、Freeze 判定）。

### 本机验证（全部串行，避免集成 flake）
```
gofmt -l .                        empty
go vet ./... / go build ./...     PASS
go test ./... -count=1            all ok（sse 4.747s）
5 个定时器相关测试 -v              5/5 PASS（新增 0.75s、改名后 2.46s）
falsify_review10.sh               14/14 ok
falsify_review10_patch41.sh       8/8  ok
falsify_review10_patch411.sh      7/7  ok（L/M/N 各自 mutated FAIL + restored PASS）
  无 FALSIFICATION 残留、无 *.orig 残留
migrate ×2                        applied（第二次干净 no-op）
db-backed package tests           execution / delivery / platform 全 ok
tests/integration                 ok 128.788s
frontend                          tsc 0 error / vitest 18 files 158 tests / vite build ok
```
`-race` 本机仍跑不了（无 gcc），由 CI 兜。工作区最终只有 4 个改动文件
（hub_test.go / falsify 脚本 / 4.1.1 报告 / .workbuddy/memory）。

### MEMORY.md 分工重构
MEMORY.md 之前 ~13.3KB，注入时尾部会被截断。本轮**结构性拆分**：
- `.workbuddy/memory/MEMORY.md`（9.2KB，注入层）只留代码不变量：当前状态、三条操作红线、
  SSE Hub 冻结规则、核心硬性约定、历轮索引。
- `.workbuddy/memory/PITFALLS.md`（6.5KB，按需读）本机环境/命令/CI 映射/flake/数据层踩坑。
MEMORY.md 顶部加指针。以后再涨就按这个边界加内容，不要再往 MEMORY.md 里堆操作细节。

### 追加：CI 红了而本地全绿 —— 定位到一处**既有** flake
首次推送 `00d7ef4` 后 CI run 35077631425 的 `check` job 在 **step 9 `go test (unit)`** 判红
（`integration` 全绿），而本机同一条 `go test ./... -count=1` 全绿。**关键手法**：
用 `GOMAXPROCS=2` 贴近 GitHub runner 的 2 vCPU 再跑（本机核多，不限制就复现不出来）。

```
GOMAXPROCS=2 压测 sse 包 12 次 → 1 次 FAIL
--- FAIL: TestHubManagerSharesOneUpstreamPerRun
    hub_test.go:332: UpstreamCount = 0, want 1
```

**先证明与本次改动无关**：`git diff 8168e5c HEAD -- hub_test.go | grep -c <测试名>` = 0
（该测试体在本批完全没被碰过）→ 既有 flake，上一次 run 只是没抽到。

根因（**测试自身的采样竞态，生产正确**）：`fakeUpstream.Subscribe` 在**返回前**就自增
`subscribes`；而 `runUpstream` 是在它之后**再取一次 `hub.mu`** 才置 `h.upstreamUp = true`。
所以 `waitFor(SubscribeCount()==1)` 退出后的**下一次裸采样**可能读到 `upstreamUp == false`。

修法：在断言**之前**补一条有界 `waitFor(mgr.UpstreamCount() == 1)` —— **原断言行一字未改**，
因此不算"放宽"（upstream 永不变 active 时 `waitFor` 照样失败）。验证：修后压测 16 次 0 失败，
全量 `go test ./...` 在 `GOMAXPROCS=2` 下跑 3 次 0 失败。提交 `6a90295`。

### 教训（已写进 PITFALLS.md）
- CI 红 / 本地绿，**第一步就把 GOMAXPROCS 限到 2** 再复现；本机核数会掩盖调度类 flake。
- 看到疑似 flake，**先 `git diff <上一次绿> HEAD -- <文件>` 证明它是不是自己引入的**，
  再谈修不修。
- 本次 4 个提交：`91633ca` 测试与文档 → `00d7ef4` 记忆 → `6a90295` flake 修复。
  最终 HEAD `6a90295` 已推送并修好本地 remote ref。

## 第十轮 Batch 5 完成（Worker Dispatcher，FROZEN，已推送 dev `54e8d31`）
按执行文档（Downloads/potal 第十轮 Batch 5…md）STEP 1–16 全部执行完毕，7 个 commit
（`44a9f79` registry → `0766196` 测试 → `98d4d28` app 注册 → `20e3bc1` cmd/worker 去 Aily 硬编码 →
`6eb0d0e` metrics → `2861a1d` 反证+race gate → `54e8d31` 报告）。报告：
`docs/potal 第十轮 Batch 5 Worker Dispatcher 整改变更报告.md`。
- 新包 `internal/workerdispatch`：Registry/ProviderSpec/Plan/providerScopedDispatcher/HealthFunc；
  错误码 `worker_provider_mismatch` / `runtime_route_snapshot_mismatch` / `runtime_handler_unavailable`；
  `studio_worker_dispatch_total{provider,runtime_type,result}`（封闭枚举）。
- app.go：`WorkerDispatch` 字段 + 注册 `feishu_aily:agent → ailyExecutor`（ailySlots 提为变量，
  与 `ProviderSlots` 兼容别名同一对象）；cmd/worker：ResolveProvider 失败 exit 2（先于任何消费），
  monitor 泛化 `plan.Provider/Slots/Health`，delivery 不再采样 Aily。
- 验证：全量 go test / migrate×2 / integration 145s / 启动验收 3 case / frontend gates
  （tsc+vitest 158+vite build）全绿；新反证 15/15。
- **已知既有偏差（非本批）**：`falsify_review9_patch33.sh` Mutation 4 锚点失配
  （Batch 4 重构 sse.go 后遗留，前 3 个 mutation 仍正常），待单独小补丁同步。
- **本机缺陷再确认**：push 成功后 `refs/remotes/origin/dev` 未写（误报 ahead 7），
  按惯例 `mkdir -p .git/refs/remotes/origin` + loose ref + packed-refs 双写修复。
- **踩坑**：Windows 的 `go build -o /tmp/...` 不认 Git Bash 虚拟路径（写到 `C:\tmp`），
  临时产物一律放 `backend-go/tmp-*`；另外同一文件并行发两个 Edit 实测丢一次写入
  （dispatcher_test.go 的 testLogger 替换被覆盖），必须串行改 + grep 复核。

## 第十轮 Batch 5.1 完成（Freeze Gate Closure，FINAL FROZEN）
按复审执行报告（Downloads/potal 第十轮 Batch 5 最新代码复审暨 5.1…md）关闭全部 4 个 P2：
- **P2-1** CI integration gate 加 `./internal/app/...`（此前 `TestBuildRegistersFeishuAilyAgentDispatchPlan`
  unit job 恒 SKIP、integration job 不列包 = CI 从未真跑）。
- **P2-2** `isNilLike`（reflect）硬化 RegisterProvider：typed-nil handler/`HealthFunc(nil)`/nil sink
  全部 boot 拒绝，新增 `ErrNilFailureSink`/`ErrNilHealthProbe` + 4 个测试。
- **P2-3** `TestSnapshotProviderMismatchFailsClosed`（provider guard 独立测试）+ 反证 **Mutation H**，
  batch5 反证扩为 **17/17**。
- **P2-4** 旧 `falsify_review9_patch33.sh` Mutation 4 只改脚本重定向到 `Subscriber.accepts`
  （hub_subscription.go）+ `TestHubSubscriberProtocolIsolation`，恢复 **8/8**，SSE 生产零改动。
- 验证：全量 go test / DB-backed 包（含 app）/ integration 153s / 反证 17/17 + 8/8 全绿。
- **lifecycle ordering 特征已记录**（Dispatcher 检查在 run.started/slots admission 之后，非本批引入，
  不重构；要提前终止需单独 Preflight 设计）。
- 6 个 commit 按 §18 拆分推送 dev；报告补 §7（含 AC-5-22 表述修正）。
- **CI 红一次并修复（de3da86）**：integration gate 纳入 app 包后 4s 失败——CI 只有 DB/Redis env
  无 .env.local，`Build` 在 `crypto.NewAESGCM("")` 爆炸。修复：测试内 `t.Setenv` 一次性
  `TOKEN_ENCRYPTION_KEY` + 钉死 `APP_ENV=development`。教训：**任何在 CI 里调 `app.Build` 的
  测试都必须自带最小 env**（CI integration job 永远没有业务密钥）。最终 run `de3da86`
  check（含 workerdispatch race gate）+ integration 双 success，Batch 5/5.1 FINAL FROZEN。
